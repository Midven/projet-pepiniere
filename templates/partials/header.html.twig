<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
	<a class="navbar-brand" href="#">Pépinière</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarColor01">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item active">
				<a class="nav-link" href="{{ path('home') }}">
					<i class="fas fa-home"></i>
					Accueil
					<span class="sr-only">(current)</span>
				</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('articles_index') }}">
					<i class="fab fa-pagelines"></i>
					Nos articles</a>
			</li>
			{% if app.user %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('compte_modification') }}">
						<i class="fas fa-user-alt"></i>
						Mon compte</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('panier_global') }}">
						<i class="fas fa-shopping-cart"></i>
						Mon Panier</a>
				</li>
				{% for role in app.user.roles %}
					{% if role == 'ROLE_ADMIN' %}
						<li class="nav-item">
							<a class="nav-link" href="{{ path('admin') }}">
								<i class="fas fa-lock"></i>
								Administration</a>
						</li>
					{% endif %}
				{% endfor %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('compte_deconnexion') }}">
						<i class="fas fa-sign-out-alt"></i>
						Déconnexion</a>
				</li>
			{% endif %}
		</ul>
	</div>
</nav>


{% for label, messages in app.flashes %}
	<div class="container">
		<div class="alert alert-{{ label }} mt-2 mb-2">
			{% for message in messages %}
				<p>{{ message | raw }}</p>
			{% endfor %}
		</div>
	</div>
{% endfor %}
